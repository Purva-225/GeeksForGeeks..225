class Solution {
  public:
    vector<int> prefSum(vector<int> &arr) {
       vector <int> ans(arr.size());
       int prefixSum = 0;
       for(int i=0; i<arr.size(); i++){
           prefixSum += arr[i];
           ans[i] = prefixSum;
        }
        return ans;
    }
};
